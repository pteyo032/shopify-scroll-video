# This repo is a fragment of the Horizon theme (a section + block), not a full
# theme. MissingTemplate flags Horizon-native snippets (contrast-override,
# spacing-style) that a real Horizon theme provides — not shipped here on
# purpose.
extends: theme-check:recommended

MissingTemplate:
  enabled: false
